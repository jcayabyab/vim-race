import React from "react";

export default function PlayerStatus({ user, userState }) {
  // row of player info
  return (
    <div>
      <div>user.username</div>
      <div>user.userState</div>
    </div>
  );
}
